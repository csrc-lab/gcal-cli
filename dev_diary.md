## 0408

### DayView
```cpp
void dayViewFormatter(map<tm, vector<tuple<tm, tm, string>>>); // map<date, vector<tuple<start, end, event>>> for events (event has start and end)
void dayViewFormatter(map<tm, vector<string>); // map<date, vector<string>> for tasks
```

