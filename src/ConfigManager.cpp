#include "ConfigManager.h"
#include <iostream>

void ConfigManager::showConfiguration() {
    // Implement the logic to show configuration
    std::cout << "Show the current configuration" << std::endl;
}

void ConfigManager::setConfiguration(const std::string &config) {
    // Implement the logic to set configuration
    std::cout << "Set the configuration: " << config << std::endl;
}
